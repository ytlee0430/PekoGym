/// Enum representing the type of an item.
/// Pure Dart — zero Flutter dependency.
enum ItemType {
  /// Pauses rest timer without evaluation penalty.
  potion,

  /// Restores 50% of max PP.
  ether,

  /// Grants move evolution experience.
  rareCandy,
}
